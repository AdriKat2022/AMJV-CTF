public class Sand : Tile
{
    // Start is called before the first frame update
    void Start()
    {
        tileType = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
